type: google.api.Service
config_version: 3

http:
  rules:
  - selector: republique.GameService.Echo
    post: /v1/echo
    body: "*"
  - selector: republique.GameService.Version
    get: /v1/version
